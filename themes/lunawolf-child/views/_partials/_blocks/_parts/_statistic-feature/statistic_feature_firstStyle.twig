{% set title     = block.title %}
{% set number    = block.stat_value %}
{% set desc      = block.desc %}
{% set aboveText = block.above_text %}

{% set text_color  = settings.text_color %}

<div class="wrapper color-{{ text_color ?: 'primary' }}">
    <div class="m__statistic__inner">
        {% if title %}
            <div class="m__statistic__top">
                <div class="titleWrapper">
                    <h1 class="m__statistic__top__title">{{ title }}</h1>
                </div>
            </div>
        {% endif %}
        {% if number or desc %}
            <div class="m__statistic__bot">
                <div class="m__statistic__bot__inner">
                    {% if number %}
                        <div class="m__statistic__bot__inner__numb">
                            <span class="count-to" data-to="{{number}}" data-duration="2000">{{ number }}</span>%
                            
                        </div>
                    {% endif %}
                    {% if desc %}
                        <h3 class="m__statistic__bot__inner__desc">{{ desc }}</h3>
                    {% endif %}
                </div>
            </div>
        {% endif %}
    </div>
</div>